apiVersion: v1
kind: Service
metadata:
  name: devsecops-service
  labels:
    app: devsecops-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: devsecops-app
